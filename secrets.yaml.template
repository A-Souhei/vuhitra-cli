# Secrets configuration template for vuhitra-cli
# Copy this file to secrets.yaml and update with your actual secrets

# Redis configuration
redis:
  # Redis password for authentication (REQUIRED)
  password: "devpassword123"
